package com.casaDoCodigo.Nicolle.Livro;

public class Markdown {
	/*
	 * Tentando entender Markdown e habilitção/configuração com atlassian commonmark //ps:quais são as importações corretas? I don't know, HELP
	 * 
	private static List<Extension> extensions =  Arrays.asList(AutolinkExtension.create());//extensão em autolink do java (???)
	private static Parser parser = Parser.builder().extensions(extensions).build();
	private static HtmlRenderer renderer = HtmlRenderer.builder().build();
	
	//@param texto em markdown
	//@return
	
	public static String renderHtml(String text) {
		Node document = parser.parse();
		return renderer.render(document);
	}
	
	public static void main (String[] args) {
		String text = "Esse é o título do livro" + "esse é o capitulo" + "esse é o sumário" + "ele tem esa informação diferente";
		System.out.println(Markdown.renderHtml(text));
	}
	*/

}
